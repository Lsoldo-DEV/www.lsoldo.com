{# {% extends 'base.html.twig' %}

{% block title %}Edit Comment{% endblock %}

{% block body %}
    <h1>Edit Comment</h1>

    {{ include('comment/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_comment_index') }}">back to list</a>

    {{ include('comment/_delete_form.html.twig') }}
{% endblock %} #}
{{ form_start(commentForm, {'action': path('app_comment_edit', {'id': comment.id})}) }}
<div class="input-group mb-3">
	{{ form_widget(commentForm.content, {'attr': {'class': 'form-control', 'placeholder': 'add_comment'|trans}}) }}
	{{ form_errors(commentForm.content) }}
	{# Afficher les erreurs de validation #}
	<button type="submit" class="btn">
		<i class="bi bi-send"></i>
	</button>
</div>
{{ form_end(commentForm) }}
