<nav class="navbar navbar-expand-lg navbar-light fixed-top">
	<div class="container">
		<a class="navbar-brand " href="{{ path('app_home') }}"><img src="{{ asset('logo_tls.webp') }}" class="logo" alt="LOGO"></a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
			<i class="bi bi-list" id="menu"></i>
		</button>
		<div class="collapse navbar-collapse justify-content-between" id="navbarNav">
			<ul class="navbar-nav ms-auto" id="navbar">
				<li class="nav-item">
					<a class="nav-link" href="{{ path('app_home') }}">Home</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('app_about') }}">About</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('app_service_index') }}">Services</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('app_project_index') }}">portfolio</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="template/blog.html">Blog</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('app_contact') }}">contact us</a>
				</li>
				
				<li class="nav-item">
					{% if app.user %}
						{% if is_granted('ROLE_ADMIN') %}
							<a class="nav-link" href="{{ path('admin') }}">Dashboard</a>
						{% elseif is_granted('ROLE_MANAGER') %}
							<a class="nav-link" href="{{ path('app_simple_admin') }}">Dashboard</a>
						{% endif %}
					{% else %}
						<a class="nav-link" href="{{ path('app_login') }}">Login</a>
					{% endif %}
				</li>

			</ul>
			<div class="d-flex ms-auto">
				<a class="btn" href="template/contact.html">Demander un devis</a>
				<button id="mode-toggle" class="btn-light-mode switch-button">
					<i id="mode-icon" class="bi bi-moon-fill"></i>
				</button>
			</div>
		</div>
	</div>
</nav>
